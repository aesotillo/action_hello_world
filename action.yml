name: 'Hola Mundo'
description: 'Saludar a alguien y guardar la fecha'
inputs:
  who-to-greet:
    description: 'A quien saludar'
    required: true
    default: 'Mundo'
outputs:
  time:
    description: 'Fecha'
runs:
  using: 'docker'
  image: docker://820832395314.dkr.ecr.eu-central-1.amazonaws.com/test-action:latest
  args:
    - ${{ inputs.who-to-greet }}
